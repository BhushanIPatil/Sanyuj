# Optional secrets for Edge Functions (set in Supabase Dashboard → Edge Functions → Secrets)
# Do not commit real values.
#
# Phone OTP edge functions are retired (return 410). Public account APIs are retired. Only the admin webapp uses authentication.

# Usually auto-injected by Supabase when functions run:
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_ANON_KEY=
